language: java

sudo: required

before_install:
 - chmod +x ./gradlew
 - chmod +x ./gradle/wrapper/gradle-wrapper.jar

jdk:
  - oraclejdk8

notifications:
  email:
    on_success: never
    on_failure: always
